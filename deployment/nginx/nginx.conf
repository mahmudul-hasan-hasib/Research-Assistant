# Insight reverse proxy — placeholder skeleton.
# TLS, HSTS, SSE passthrough and rate limits are implemented in Phase 1/9.
# See docs/ARCHITECTURE.md Part 13.4.

# upstream backend { server backend:8000; }
# upstream frontend { server frontend:3000; }

server {
    listen 80;
    # server_name insight.example.com;

    # location /api/v1/streams/ { proxy_pass http://backend; proxy_buffering off; }
    # location /api/ { proxy_pass http://backend; }
    # location / { proxy_pass http://frontend; }
}
